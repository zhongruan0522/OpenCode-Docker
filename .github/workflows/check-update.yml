name: Check Updates

on:
  schedule:
    # 北京时间 8:00 / 12:00 / 20:00
    - cron: '0 0 * * *'
    - cron: '0 4 * * *'
    - cron: '0 12 * * *'
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    permissions:
      actions: write
      contents: read

    steps:
      # ==========================================
      # 获取上游最新版本
      # ==========================================
      - name: Get OpenCode latest release
        id: opencode
        run: |
          LATEST=$(curl -s https://api.github.com/repos/anomalyco/opencode/releases/latest | jq -r '.tag_name')
          echo "version=$LATEST" >> $GITHUB_OUTPUT
          echo "OpenCode latest: $LATEST"

      - name: Get code-server latest release
        id: codeserver
        run: |
          LATEST=$(curl -s https://api.github.com/repos/coder/code-server/releases/latest | jq -r '.tag_name')
          # code-server 的 tag 带 v 前缀，剥离后存纯版本号
          echo "version=${LATEST#v}" >> $GITHUB_OUTPUT
          echo "code-server latest: $LATEST"

      - name: Get Codex latest release
        id: codex
        run: |
          LATEST=$(curl -s https://api.github.com/repos/openai/codex/releases/latest | jq -r '.tag_name')
          # Codex 的 tag 格式为 rust-v{version}，剥离前缀
          echo "version=${LATEST#rust-v}" >> $GITHUB_OUTPUT
          echo "Codex latest: $LATEST"

      - name: Get serena-agent latest version
        id: serena
        run: |
          LATEST=$(curl -s https://pypi.org/pypi/serena-agent/json | jq -r '.info.version')
          echo "version=${LATEST}" >> $GITHUB_OUTPUT
          echo "serena-agent latest: $LATEST"

      - name: Get Claude Code latest version
        id: claudecode
        run: |
          LATEST=$(curl -s https://registry.npmjs.org/@anthropic-ai/claude-code/latest | jq -r '.version')
          echo "version=$LATEST" >> $GITHUB_OUTPUT
          echo "Claude Code latest: $LATEST"

      # ==========================================
      # 获取我们上次记录的版本（从 release body 提取）
      # ==========================================
      - name: Get our current versions
        id: local
        run: |
          BODY=$(curl -s https://api.github.com/repos/${{ github.repository }}/releases/latest | jq -r '.body // empty')
          if [ -n "$BODY" ]; then
            LOCAL_OPENCODE=$(echo "$BODY" | grep -oP 'opencode-version=\K[^\s]+' || true)
            LOCAL_CODESERVER=$(echo "$BODY" | grep -oP 'code-server-version=\K[^\s]+' || true)
            LOCAL_CODEX=$(echo "$BODY" | grep -oP 'codex-version=\K[^\s]+' || true)
            LOCAL_SERENA=$(echo "$BODY" | grep -oP 'serena-version=\K[^\s]+' || true)
            LOCAL_CLAUDECODE=$(echo "$BODY" | grep -oP 'claude-code-version=\K[^\s]+' || true)
          fi
          echo "opencode=${LOCAL_OPENCODE:-none}" >> $GITHUB_OUTPUT
          echo "codeserver=${LOCAL_CODESERVER:-none}" >> $GITHUB_OUTPUT
          echo "codex=${LOCAL_CODEX:-none}" >> $GITHUB_OUTPUT
          echo "serena=${LOCAL_SERENA:-none}" >> $GITHUB_OUTPUT
          echo "claudecode=${LOCAL_CLAUDECODE:-none}" >> $GITHUB_OUTPUT
          echo "Our OpenCode: ${LOCAL_OPENCODE:-none}"
          echo "Our code-server: ${LOCAL_CODESERVER:-none}"
          echo "Our Codex: ${LOCAL_CODEX:-none}"
          echo "Our serena-agent: ${LOCAL_SERENA:-none}"
          echo "Our Claude Code: ${LOCAL_CLAUDECODE:-none}"

      # ==========================================
      # 比对版本，任意一个有更新就触发构建
      # ==========================================
      - name: Compare versions
        id: compare
        run: |
          HAS_UPDATE="false"

          # OpenCode 对比
          if [ "${{ steps.local.outputs.opencode }}" != "${{ steps.opencode.outputs.version }}" ]; then
            HAS_UPDATE="true"
            echo "OpenCode needs update: ${{ steps.local.outputs.opencode }} -> ${{ steps.opencode.outputs.version }}"
          else
            echo "OpenCode up to date: ${{ steps.opencode.outputs.version }}"
          fi

          # code-server 对比
          if [ "${{ steps.local.outputs.codeserver }}" != "${{ steps.codeserver.outputs.version }}" ]; then
            HAS_UPDATE="true"
            echo "code-server needs update: ${{ steps.local.outputs.codeserver }} -> ${{ steps.codeserver.outputs.version }}"
          else
            echo "code-server up to date: ${{ steps.codeserver.outputs.version }}"
          fi

          # Codex 对比
          if [ "${{ steps.local.outputs.codex }}" != "${{ steps.codex.outputs.version }}" ]; then
            HAS_UPDATE="true"
            echo "Codex needs update: ${{ steps.local.outputs.codex }} -> ${{ steps.codex.outputs.version }}"
          else
            echo "Codex up to date: ${{ steps.codex.outputs.version }}"
          fi

          # serena-agent 对比
          if [ "${{ steps.local.outputs.serena }}" != "${{ steps.serena.outputs.version }}" ]; then
            HAS_UPDATE="true"
            echo "serena-agent needs update: ${{ steps.local.outputs.serena }} -> ${{ steps.serena.outputs.version }}"
          else
            echo "serena-agent up to date: ${{ steps.serena.outputs.version }}"
          fi

          # Claude Code 对比
          if [ "${{ steps.local.outputs.claudecode }}" != "${{ steps.claudecode.outputs.version }}" ]; then
            HAS_UPDATE="true"
            echo "Claude Code needs update: ${{ steps.local.outputs.claudecode }} -> ${{ steps.claudecode.outputs.version }}"
          else
            echo "Claude Code up to date: ${{ steps.claudecode.outputs.version }}"
          fi

          echo "has_update=$HAS_UPDATE" >> $GITHUB_OUTPUT

      - name: Trigger build
        if: steps.compare.outputs.has_update == 'true'
        uses: actions/github-script@v8
        with:
          script: |
            await github.rest.actions.createWorkflowDispatch({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: 'docker-build.yml',
              ref: 'main',
              inputs: {
                opencode_version: '${{ steps.opencode.outputs.version }}',
                code_server_version: '${{ steps.codeserver.outputs.version }}',
                codex_version: '${{ steps.codex.outputs.version }}',
                serena_version: '${{ steps.serena.outputs.version }}',
                claude_code_version: '${{ steps.claudecode.outputs.version }}'
              }
            });
